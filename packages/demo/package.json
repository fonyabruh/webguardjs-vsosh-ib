{
  "name": "@webguard/demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build -w @webguard/detector-web && esbuild src/index.ts --bundle --sourcemap --format=iife --platform=browser --outfile=dist/demo.js && node scripts/copy-html.js"
  },
  "dependencies": {
    "@webguard/detector-web": "0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
